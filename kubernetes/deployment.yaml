apiVersion: apps/v1
kind: Deployment
metadata:
  name: pigen
  labels: {app: pigen}
spec:
  replicas: 2
  selector:
    matchLabels: {app: pigen}
  template:
    metadata:
      labels: {app: pigen}
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: pigen
          image: pigen:latest # placeholder — the deployer sets the real image in their Argo CD app (kustomize images)
          ports:
            - {name: pi-tcp, containerPort: 31415, protocol: TCP}
            - {name: pi-udp, containerPort: 31415, protocol: UDP}
            - {name: http, containerPort: 8080, protocol: TCP}
          env:
            # Cap digits per connection in shared clusters; 0 = unlimited per RFC 3091
            - {name: PIGEN_MAX_DIGITS, value: "1000000"}
            # Pause between digits on the browser (SSE) stream, milliseconds
            - {name: PIGEN_WEB_PACE_MS, value: "75"}
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 5
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            periodSeconds: 10
          resources:
            requests: {cpu: 50m, memory: 32Mi}
            limits: {cpu: 500m, memory: 128Mi}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
      terminationGracePeriodSeconds: 15
